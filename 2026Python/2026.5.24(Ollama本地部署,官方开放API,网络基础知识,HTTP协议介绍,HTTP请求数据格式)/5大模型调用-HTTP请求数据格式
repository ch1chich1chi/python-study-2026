HTTP协议-请求数据格式:
第一行:请求行(请求方式 资源路径 协议)
请求方式:
    GET:请求参数在请求行中,没有请求体.如:/api/courses?name=Python&status=1.GET请求请求参数大小在浏览器中是有限制的
    ?之后的一部分是携带的一些参数的信息,表现形式就是k=value &叫与,and
    POST:请求参数在请求体中,POST请求大小是没有限制的

第二行开始:请求头,格式key:value

请求头和请求体中间有一个空行
空行之后:请求体(请求参数部分,GET方式没有,POST可以有,key:value)

HTTP协议-响应数据格式:
第一行:响应行(协议 状态码)
状态码:
    200:客户端请求成功
    400:请求参数错误
    404:请求资源不存在,url输入有误,或者网站资源被删了
    500:服务器发生了不可预期的错误

第二行开始:响应头,格式key:value

响应头和响应体中间有一个空行
空行之后:响应头(存放服务端响应的数据)